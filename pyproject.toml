[project]
name = "ai-knowledge-assistant"
version = "0.1.0"
description = "AI Knowledge Assistant API"
requires-python = ">=3.11,<3.13"

[tool.black]
line-length = 88

[tool.isort]
profile = "black"

[tool.flake8]
max-line-length = 88
exclude = [".venv", "__pycache__", "build", "dist"]
